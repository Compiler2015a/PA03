class A {
  string str;
  int i;

  static int sfunc(boolean b) {
    if (b) {
      {
		int[] l;
      }
      return 1;
    }
    else {
      A x = new A();
      x.i = 9;
      x.str = "hello";
      return 0;
    }
  }

  void vfunc(A a, int max, int val) {
    int[] arr = new int[max];
    int i = 0;
    while (i < max){
        arr[i] = val;
        i = i + 1;
        Library.printi(i);
        if (a == null)
          break;
    }
  }
}

class B extends A {
    int j;

    void vfunc(A a, int max, int val) {
		int[][] twoArr = new int[][this.j];
		int[] oneArr = new int[3];
		while (true) {
			if (false) {
				while (j != oneArr[2]) {
					if (!(j == j- (-j+3 % oneArr.length)/(j+j*2))) {
						j = 4; }
					else
							while (true) { j = this.j +twoArr.length; }
				} 
				string g = "222";
				{
					string[][] twoArr2 = new string[][this.j];
					string[] oneArr2 = new string[3];
					twoArr2[1] = oneArr2;
					oneArr2[oneArr2.length] = g + twoArr2[1][5];
				}
			}
		}
		twoArr[1] = oneArr;
    }
}

class C {
  static void main(string[] args) {
    A[][] a;
  }
}
